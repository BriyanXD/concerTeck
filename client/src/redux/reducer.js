const initialState = {
  Events: [],
  BigEvents: [],
  Genres: [],
  Detail: {},

};

function reducers(state = initialState, {type, payload}) {
  switch (type) {
    default:
      return state;
  }
}
export default reducers;
