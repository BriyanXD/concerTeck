const initialState = {
  Events: [],
  BigEvents: [],
  Detail: {},

};

function reducers(state = initialState, {type, payload}) {
  switch (type) {
    default:
      return state;
  }
}
export default reducers;
